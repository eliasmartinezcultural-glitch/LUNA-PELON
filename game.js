const menu=document.getElementById('menu'),game=document.getElementById('game'),play=document.getElementById('playButton'),player=document.getElementById('player'),interaction=document.getElementById('interaction'),dialogue=document.getElementById('dialogue');const keys={};let x=.5,y=.5;const npcs=[{x:.42,y:.42,name:'Vecino',text:'Buen día. Bienvenido a Luna Pelón.'},{x:.73,y:.28,name:'Luna',text:'Hola. ¿Recién llegás?'}];play.addEventListener('click',()=>{menu.classList.add('hidden');game.classList.remove('hidden')});addEventListener('keydown',e=>{const k=e.key.toLowerCase();keys[k]=true;if(['arrowup','arrowdown','arrowleft','arrowright',' '].includes(k))e.preventDefault()});addEventListener('keyup',e=>keys[e.key.toLowerCase()]=false);document.querySelectorAll('.touch-controls button').forEach(b=>{const k=b.dataset.key;const down=e=>{e.preventDefault();keys[k]=true};const up=e=>{e.preventDefault();keys[k]=false};b.addEventListener('pointerdown',down);b.addEventListener('pointerup',up);b.addEventListener('pointercancel',up);b.addEventListener('pointerleave',up)});function loop(){let dx=0,dy=0;if(keys.w||keys.arrowup)dy-=.004;if(keys.s||keys.arrowdown)dy+=.004;if(keys.a||keys.arrowleft)dx-=.004;if(keys.d||keys.arrowright)dx+=.004;if(dx&&dy){dx*=.707;dy*=.707}x=Math.max(.03,Math.min(.97,x+dx));y=Math.max(.06,Math.min(.94,y+dy));player.style.left=x*100+'%';player.style.top=y*100+'%';let nearest=null,best=.09;npcs.forEach(n=>{const d=Math.hypot(x-n.x,y-n.y);if(d<best){best=d;nearest=n}});if(nearest){interaction.textContent='E — Hablar con '+nearest.name;interaction.style.display='block';if(keys.e){dialogue.textContent=nearest.name+': '+nearest.text;dialogue.style.display='block';keys.e=false}}else{interaction.style.display='none';dialogue.style.display='none'}requestAnimationFrame(loop)}loop();