const menu=document.getElementById('menu');const game=document.getElementById('game');const play=document.getElementById('playButton');const player=document.getElementById('player');const world=document.getElementById('world');const keys={};let x=.5,y=.5;play.addEventListener('click',()=>{menu.classList.add('hidden');game.classList.remove('hidden');});addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=true});addEventListener('keyup',e=>{keys[e.key.toLowerCase()]=false});function loop(){let dx=0,dy=0;if(keys.w||keys.arrowup)dy-=.004;if(keys.s||keys.arrowdown)dy+=.004;if(keys.a||keys.arrowleft)dx-=.004;if(keys.d||keys.arrowright)dx+=.004;x=Math.max(.03,Math.min(.97,x+dx));y=Math.max(.06,Math.min(.94,y+dy));player.style.left=(x*100)+'%';player.style.top=(y*100)+'%';requestAnimationFrame(loop)}loop();