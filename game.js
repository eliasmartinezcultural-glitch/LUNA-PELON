const menu=document.getElementById('menu');const game=document.getElementById('game');const play=document.getElementById('playButton');const player=document.getElementById('player');const world=document.getElementById('world');const interaction=document.getElementById('interaction');const keys={};let x=.5,y=.5;const npcs=[{x:.42,y:.42,name:'Vecino'},{x:.73,y:.28,name:'Luna'}];play.addEventListener('click',()=>{menu.classList.add('hidden');game.classList.remove('hidden');player.focus()});addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=true;if(['arrowup','arrowdown','arrowleft','arrowright',' '].includes(e.key.toLowerCase()))e.preventDefault()});addEventListener('keyup',e=>{keys[e.key.toLowerCase()]=false});function loop(){let dx=0,dy=0;if(keys.w||keys.arrowup)dy-=.004;if(keys.s||keys.arrowdown)dy+=.004;if(keys.a||keys.arrowleft)dx-=.004;if(keys.d||keys.arrowright)dx+=.004;if(dx&&dy){dx*=.707;dy*=.707}x=Math.max(.03,Math.min(.97,x+dx));y=Math.max(.06,Math.min(.94,y+dy));player.style.left=(x*100)+'%';player.style.top=(y*100)+'%';let nearest=null,best=.09;npcs.forEach(n=>{const d=Math.hypot(x-n.x,y-n.y);if(d<best){best=d;nearest=n}});if(nearest){interaction.textContent='E — Hablar con '+nearest.name;interaction.style.display='block';if(keys.e){interaction.textContent=nearest.name==='Luna'?'Luna: Hola. ¿Recién llegás?':'Vecino: Buen día. Bienvenido a Luna Pelón.';keys.e=false}}else interaction.style.display='none';requestAnimationFrame(loop)}loop();